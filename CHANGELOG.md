# 改版歷史

- **2026-08-17（三續）**：**成套推倒 model 切換＋審核閘，返去 08-17 之前原狀，由頭諗過** 起因：睇完三向 A/B 測試結果（見下面「續」條），Stephanie 話「淨係想成套推倒，由頭諗過」——即係唔要淨改返 `reasoningEffort`／換 model 呢啲微調，連同今日加嘅審核閘制度一齊推翻，返去今日改之前嘅狀態再諗過個方向。**做法**：由呢個 session 自己嘅對話記錄（每個檔改之前嘅第一次 `Read` 結果）逐字攞返原文，唔靠估記憶，逐個檔核對過冇夾雜今日改動先寫返去真 Mac：`src/lib/deepseek.ts`（拆走 `thinking`／`reasoningEffort` opts，返去淨得 `model` opt，`thinking` 硬寫死 `disabled`）、`src/app/api/cron/generate-stories/route.ts`（generateOne 同 selfCheckClosure 兩個 call 都返去 `model:"deepseek-chat"`，`maxTokens` 返 6000，`timeoutMs` 返 120000，insert `novel_stories` 唔再帶 `status` 欄）、`src/app/page.tsx`／`browse/page.tsx`／`serial/page.tsx`／`short/page.tsx`／`categories/page.tsx`（拆走 `.eq("status","published")` 濾走）、`src/app/actions/admin.ts`（拆走 `approveStory`／`rejectStory`，淨返 `deleteStory`）、`src/app/admin/page.tsx`（返去單一故事列表，唔再分待審/已發布/已拒絕三段）、`novel-web/CLAUDE.md`（「Scheduled generation」段返去無審核閘描述）；刪走新增嘅 `src/components/PendingStoryReview.tsx`；Supabase migration `revert_novel_stories_status_review_gate` drop 走 `novel_stories.status` 欄（連 CHECK constraint）。**效應**：之前手動撥返 `pending` 嘅兩篇「補漏」故事（`0978a4d3`／`b0ff3a30`，13:16 HKT 舊 pipeline 出品）因為冇咗 status 欄，會同其他故事一樣正常喺公開頁出現——呢個係推倒審核閘嘅必然效應，唔係新決定，如果唔想呢兩篇曝光要另外手動刪走。**核實**：真 Mac `npx tsc --noEmit` 過；`github_push.py --check` preflight 確認改動嘅 10 個檔＋1 個刪走檔同預期完全一致，冇夾雜第三方改動；push 後 GitHub API 核實 HEAD。**未做**：① 內容生成方向仲未有新諗法——今次淨係推倒返舊狀態，AI腔/老土/唔夠打動/邏輯牽強四個原始投訴依然未解決，等 Stephanie 諗清楚新方向先再動手；② `public.service_heartbeat`／`public.brain_chunks` RLS 未開呢單獨立事項同今次推倒無關，繼續開住等 Stephanie 拍板；③ 三個 A/B 測試樣本（`/tmp/novel_ab_test_low_vs_disabled.md` 等）純本機測試檔案，未入 repo，呢部 Mac 先有，如果之後唔會再用可以刪。

- **2026-08-17（續）**：**出街前三向 A/B 測試：撞到 `reasoningEffort:"high"` 爆 token 預算得0字嘅事故 + Stephanie 提議轉 v4-flash 慳成本，都測咗，最終企硬用 `deepseek-v4-pro`+`reasoning_effort:"low"`** 起因：加完審核閘之後，Stephanie 問「幾錢」「low 幾錢、quality 點」，順勢喺真 API 度用同一個 prompt（骨架/女主男主/skeleton 全部一樣，淨換 model/thinking 設定）連續三次 call 對比。**發現一（自己測到，未上線就截住）**：`deepseek-v4-pro` + `reasoning_effort:"high"` 會將 `maxTokens:6000` 嘅預算**全部**畀 reasoning 用晒（`completion_tokens_details.reasoning_tokens` 同 `completion_tokens` 係共用同一個上限，唔係額外計），正文 0 字、parse fail——即係如果冇試過就直接部署，聽日 12:30 HKT 個 cron 會日日生成空故事。已經改用 `reasoning_effort:"low"`（reasoning 淨用 300-500 token，正文照樣寫齊，`maxTokens` 順手加到 7500 留返 serial 嘅位），commit `be1d163`，`tsc --noEmit` 過，已 push＋核實 HEAD。**發現二（Stephanie 提議測嘅）**：問「而家個 bot 用緊嘅 deepseek-v4-flash thinking 已經 open，不如繼續用呢個」，合理嘅慳成本諗法——實測 `deepseek-v4-flash`+`thinking:enabled`+`reasoning_effort:"low"`（同一個 prompt）**明顯倒退**：①正文夾雜大量**簡體字**（確、對、廳、错 等），會撞 `validate()` 嘅 `SIMPLIFIED_ONLY` 清單觸發 retry；②劇情返晒去「年薪百萬／寶馬三系／分紅一百二十萬」嘅**財閥炫富**寫法——正正係 08-03 你話「太商業」要求剔走嘅嗰種；③結尾破壞第四面牆：角色講出「等咗你兩章」「夠等個結局了」，模型喺敘事入面直接講「章節」呢啲 meta 概念，讀者睇會出戲。v4-flash 版平幾錢（US$0.0016/篇 vs v4-pro 嘅 US$0.0041/篇，差 US$0.0025≈HK$0.02一篇），但質素倒退，唔划算，**維持用 `deepseek-v4-pro`**，route.ts 冇再改。三個測試樣本全文存喺 `/tmp/novel_ab_test_low_vs_disabled.md`（純本機測試檔，未入 repo，得呢部 Mac 有）。**估算月費**：`deepseek-v4-pro`+low，2篇/日×30日，off-peak rate（cron 04:30 UTC 岩岩喺 peak window 之外），連 code validate retry 都計埋，大約 **HK$4-8/月**（原本舊版都係呢個水平，冇因為換model變貴好多）。

- **2026-08-17**：**novel-web 質素問題溯源：揭發正文一直冇真正用過「高質素」model + 加審核閘（pending/published/rejected），唔再自動即刻上架** 起因：Stephanie /grilling 話「小說質素仍然太低」，四項全中（AI腔生硬／情節老土／唔夠打動／揭露邏輯牽強）。查 Supabase 先發現**由 07-31 之後一篇新故事都冇存活過**——heartbeat 話 cron 日日 12:30 HKT 都仲有跑（最後 08-17 13:16），即係 08-13 嗰輪大改跑咗成幾日，Stephanie 一路照刪冇留手尾，冇留低任何樣本可以診斷（`novel_likes`/`novel_endings` cascade delete，唔係 soft delete），連上一輪改完之後嘅效果都無從得知——證明「淨改prompt再等結果」呢條路已經行過一次仲係唔掂。再查 `deepseek.ts` 先發現關鍵事故：08-01 話「故事生成改用 `deepseek-chat` 求文筆質感」，但呢個 model 名 **07-24 官方已經停用**、一路靜靜哋被動態 alias 去 `deepseek-v4-flash`——即係「換高質素model」呢個動作由第一日開始就從來冇真正生效過，一路都係用緊同結局生成一樣嘅快速/平價 tier。同時查到 DeepSeek 08-13 先 GA 咗 `deepseek-v4-pro`（正正就係上次大改嗰日，所以嗰輪都仲未有機會試）。**修**：① 正文生成（`route.ts` generateOne）正式指定 `model:"deepseek-v4-pro"` + `thinking:"enabled"`（`deepseek.ts` 加返 `thinking`/`reasoningEffort` opts，預設 disabled 唔影響其他 call；`reasoningEffort` 原本寫 `"high"`，出街前 A/B 測到會爆晒 token 預算得0字，已經改用 `"low"`，詳見下面「續」條）；`selfCheckClosure` 呢類純分類任務由已停用嘅 "deepseek-chat" 改用返正式名 "deepseek-v4-flash"，避免 alias 將來被撤走就死。② 加 `novel_stories.status` 審核閘（`pending`/`published`/`rejected`，migration `add_novel_stories_status_review_gate`，舊資料 default `published` 唔受影響）：cron insert 一律 `status:'pending'`，首頁／`/short`／`/serial`／`/browse`／`/categories` 公開查詢全部加 `.eq('status','published')` 濾走未審故事；`/admin` 新增「待審」區（`PendingStoryReview.tsx`，睇全文＋code validate 備註）俾 Stephanie 批准（→published）或者唔要（→rejected，**唔刪**，留低做「唔夠好」嘅診斷樣本，`admin.ts` 加 `approveStory`/`rejectStory`，`/admin` 頁面拆做待審／已發布／已拒絕三段）。③ 順手發現：改緊呢批期間（13:16 HKT）舊 code 嘅 cron 啱啱好又跑咗一次，新生成兩篇因為 migration 已加咗 status 但新 code 未 deploy，落地時攞咗欄位 default `published` 直接上咗架——執行 SQL 手動撥返 `pending` 補漏。**核實**：真 Mac `npm install`＋`tsc --noEmit`＋`next build` 全過；`github_push.py` 交叉 review（Codex）PASS，push commit `3374fa4`，GitHub API 核實 HEAD 一致；deploy 後 curl 生產首頁／`/short`／`/serial`／`/categories` 全部 200，兩篇補漏故事已經唔喺首頁 HTML 出現（證明 status 濾走生效）；`/admin` 未登入正確 307 redirect。⚠️ **未做**：thinking:high 令單次生成變慢，`timeoutMs` 由 120s 加到 150s，但 3 次 retry × 2 個 story type 理論上仲可能撞 300s route maxDuration 嘅 tail risk（機會微，未做深層架構修正，留意頭幾日 heartbeat／有冇成日 timeout）；今日 13:16 嗰兩篇（已撥返 pending）係**舊 pipeline**出品，唔反映 v4-pro 效果，真正第一批新 model 樣本要等 08-18 12:30 HKT 先有；`content/fable_batch_2026-07-10.md`（Voice Profile 校準錨點）依家搵唔返，將來想再校準冇晒參照，未跟進。另外查 DB 時發現 `public.service_heartbeat`／`public.brain_chunks` 兩張表 RLS 冇開（anon key 可以自由讀寫），未動，已另外知會 Stephanie。

- **2026-08-13**：**novel-web 故事生成大改：由「一條固定公式」變三個骨架 + 加共鳴/落淚同揭露機制兩條硬規 + gen_meta 防撞** 起因：Stephanie 反饋「小說唔夠好」，話 webapp 入面刪咗好多篇。查實：08-01 起 daily cron 生成嘅第一週（08-01 至 08-06）已經全數被刪，08-03 剔走財閥 slot 之後仍然不斷刪，證明單改 slot 池唔夠。診斷出四個病：①4個slot（處境/身份/事件/反轉）冇追蹤近期用過乜，短期內好易撞返差唔多組合；②`AI_CLICHES` 淨4個詞，情感位好多時直接寫「佢好感動」（telling唔係showing）；③`HERO_IDENTITIES` 8個非財富設定入面4個都用「深藏/隱藏/扮」開首，讀落單一；④成個系統淨得一種故事骨架（處境×身份反差×事件×反轉），100%故事都行「隱藏身份被揭穿」呢條線。查咗2026年短劇市場數據（番茄/抖音精選），發現市場仲有「契約婚姻先婚後愛」「雙強對峙」等完全唔同嘅骨架我哋冇涵蓋；仲用真實截圖（Threads 語錄體故事帖）對比，抽出「情感要普世共鳴、show not tell」嘅方向。透過連續幾輪手寫 demo 故事來回校準（用家逐次挑出邏輯漏動：翻手機memo唔合理、舊電話冇卡點call、記事本留咗喺度佢點記得做——每次都指向「翻舊物揭發」呢類寫法本身就係要靠好多巧合先撐得住），最終定出「即時撞破/第三者講漏嘴/直接對峙」三選一嘅揭露機制，取代所有「搵到證物」寫法。**修**：`route.ts` 加骨架系統（`identity_reveal`/`contract_marriage`/`power_clash`，5:3:2權重，各自獨立 slot 池，唔同骨架用唔同 genre tag 池）；`STYLE_2026` 加「共鳴/落淚硬規」（普世情感、情緒一律用具體動作/物件/停頓帶出唔准直寫情緒）同「揭露機制硬規」；`AI_CLICHES` 由4個詞擴充到13個；`HERO_IDENTITIES` 8個非財富設定全部重寫開首句式，減少「深藏」重複感；Supabase migration 加 `novel_stories.gen_meta`（jsonb）記低每篇用咗邊個骨架/slot，下次生成排除近8篇用過嘅組合。⚠️ 新genre tag「雙強對峙」原本已經俾 `genreCategories.ts` 攞咗做古言宮廷（period-drama）大類用（現代設定唔啱嗰個分類），所以現代雙強骨架改用新名「強強交鋒」避免撞名累到前台分錯大類；`契約婚姻`／`強強交鋒` 兩個新tag已經歸落合適嘅前台大類（`hidden-identity`／`comeback`）。寫呢條 CHANGELOG 途中先發現漏咗一步：新 genre tag 唔喺 `genreCategories.ts` 嘅 `PARENT_CATEGORIES` mapping 度會 fallback 灰色（`FALLBACK_COLOR`），即係讀者會見到兩個新故事類型冇分類色——當場補做：「契約婚姻」歸入 `hidden-identity`（馬甲身份，同「假戲真做」情緒賣點相近），「強強交鋒」歸入 `urban-life`（都市日常，同「都市隱世強者」相近）。**核實**：`tsc --noEmit` 喺真 Mac（有 node_modules）跑通過，`github_push.py` push 成功 commit `664afe1`，GitHub API 核實 HEAD 一致。實際生成質素要等下一次 cron（08-14 HKT 12:30）先見到，跟返 08-03 個做法冇手動觸發 production 避免插入額外內容。⚠️ **未做**：「年代/穿越」骨架冇加（之前提出過但用家話遲啲先做）；①②③四個病嘅④（其他）冇具體樣本，仲未跟進。

- 2026-08-03（承上一條「⚠️ 未做」手尾）：**Cowork `novel-story-generator` skill 正式標記 deprecated；同時更正我早前答錯嘅嘢** 起因：Stephanie 問「novel 今日會唔會跑 DeepSeek」，我讀咗嗰份仲未標記 deprecated 嘅 Cowork skill，答錯話「故事生成用 Claude、DeepSeek 淨係讀者揀結局先觸發」——一路查到 `novel-web/CLAUDE.md`「Scheduled generation」先發現答錯：**故事生成而家都係用 DeepSeek**（Vercel Cron `route.ts` 內），日日 HKT 12:30 都會跑一次，唔止結局先用 DeepSeek。**修**：用 `save_skill(overwrite:true)` 將個死 skill 內容換晒做 deprecation notice，指返 `route.ts`／`novel-web/CLAUDE.md` 做正本，唔再擺完整舊 prompt（避免之後又有人或 AI 睇漏眼跟錯 12:30/16:30、2 serial+1 short、30 類男女頻池嗰套舊制）。兩份 `CLAUDE.md`（root／novel-web）08-01 已經更新啱，呢次冇再改。

- 2026-08-03：**novel-story-generator 文檔漂移修正 + 剔走 HERO_IDENTITIES 豪門財閥向 slot（Stephanie 反饋故事太商業）** 起因：Stephanie 覺得故事太商業，原本想改 Cowork skill `novel-story-generator` 嘅 30 類男女頻加權池，但查實 code 先發現 **2026-08-01 已經由 Cowork scheduled task 搬咗去 Vercel Cron**（`novel-web/src/app/api/cron/generate-stories/route.ts`，`route.ts` 頂部本身有註解），舊機制已死；但 `daily-novel/CLAUDE.md`、`novel-web/CLAUDE.md`、Cowork `novel-story-generator` skill 三處都仲寫緊舊制（每日兩批 12:30/17:30、2連載1短篇、30類男女頻加權池），一直冇跟手同步，撞正會累到下次又改錯地方。**修**：① 兩份 `CLAUDE.md` 更新反映現狀——`vercel.json` cron `"30 4 * * *"`（HKT 12:30，每日一次）、code loop 固定 1 serial+1 short、4-slot 隨機組合（`HEROINE_SITUATIONS`×`HERO_IDENTITIES`×`TRIGGER_EVENTS`×`MIDPOINT_TWISTS`+`GENRE_TAGS`）已取代舊 30 類男女頻池，冇「男頻/女頻」之分；② `route.ts` `HERO_IDENTITIES` 剔走「深藏不露嘅總裁」「扮窮嘅富豪」「隱藏富商後代」「被誤認做普通職員嘅集團繼承人」「宮鬥劇裡嘅帝王/世子」「傳聞中六親不認嘅隱世家族繼承人」「表面紈絝實則深藏心機嘅世子/少爺」「被全家否定嘅庶出繼承人」共 8 個豪門財閥向 slot，換做 8 個非財富向身份反差 slot（退役特種兵／金牌外科醫生／得獎作家導演／武術冠軍／臥底特工／過氣明星／急救醫生消防員／米芝蓮主廚），保留身份反轉張力但唔靠「有錢有勢」。⚠️ **未做**：Cowork `novel-story-generator` skill（save_skill 版本）仍然係死殘留，未刪／未加 deprecated 標記，如果之後又有人（或 Claude）誤以為佢係真身會再撞板一次，建議下次一併清走或標記。`tsc --noEmit` 已驗證 `route.ts` 語法通過，實際生成效果要等下一個 12:30 HKT cron 觸發先見到（未有手動觸發，避免額外插入 production 內容）。

- 2026-08-01：**novel_endings 撞 `duplicate key ... novel_endings_user_story_uidx` 儲存失敗 → drop 舊 unique index** — 讀者揀第二個分支結局時報「儲存失敗」。查因：`novel_endings_user_story_uidx`＝`UNIQUE(user_id, story_id)`，但 2026-07-11 起 `endings.ts` 已改做「同一故事可以生成多個結局，insert 唔 upsert」，呢個 unique index 係改設計嗰陣冇跟手拆走嘅殘留，全 repo grep 冇代碼依賴。Supabase migration `drop_stale_novel_endings_user_story_uidx` drop 咗（`idx_novel_endings_user`／兩條 FK 唔變）。已用真 user_id/story_id insert 第二行測試驗證通過，再刪走測試資料。純 DB 改動，代碼／novel-web repo 冇變，唔使 push。

- 2026-08-01（承 07-31 制度複檢）：**`scripts/github_push.py` 修靜默故障** — 舊版 `_PUSH_STATE_DIR` 用 `os.path.dirname(REPO)` 當 stephanie-personal 係隔籬 folder；04-MAINTENANCE §6 將 5 個 repo 搬出 Drive Mirror 後假設崩咗，`makedirs` 靜靜咁喺 `~/Desktop/dev`、`~/dev`、`daily-novel/` 開咗 3 個假 stephanie-personal，concurrent-push 偵測對 6 個 repo 死咗都冇人知（真 state 檔停留喺 7/26–7/30）。改為 `STEPHANIE_PERSONAL_DIR` 環境變數 → Drive 正本絕對路徑 → legacy sibling 三段 resolve，搵唔到就**唔寫兼出聲**（S5「死咗邊個會知」）。12 份 script 一齊改，py_compile 全過，sales-trainer 實跑驗證真 state 有更新。假 folder 已收入 `_to_delete/`。

- 2026-07-31：push 記錄事實更正 — 「novel-web 雲端 API push 403 → `device_commit_files` 交返本機」改為「container 仍 403，但經 desktop-commander 喺真 Mac 跑就得（實測 `c319031`）」。同日 06-STANDARDS 例外表同步刪走嗰條例外。順帶補明「推 novel-web 要 cd 入去單獨推一次」。改咗 `CLAUDE.md` 同 `novel-web/CLAUDE.md`。

- 2026-07-31：`.gitignore` 加 `*.bak-*` 第二道防線 — 配合 06-STANDARDS §S3「備份一律開喺 `_to_delete/`」，就算漏咗 mv 都唔會畀 `github_push.py` 誤推上 GitHub（2026-07-25 事故嘅根治）。本 repo 冇 governance `backups/`，所以唔需要 negation 例外。

- 2026-07-31：`CLAUDE.md` ⚙️ Standards block 事實更正 — `governance 00–05（派 subagent 先讀 01+03…）` → `governance 00–06（派工跟 01 §1 門檻表，要派先抄 03 模板…）`。06-STANDARDS.md 07-18 已加入但 router 行仲寫 00–05；「先讀 01+03」07-31 鬆綁（為派一個 agent 先燒 230 行本身係 context 稅）。正本改動見 stephanie-personal CHANGELOG 同日條目。

- 2026-07-31：repo 搬出 Google Drive Mirror，新位置 `~/Desktop/dev/daily-novel`。原因：node_modules 嘅 npm `.bin` symlink 令 Drive 持續報 sync error。換機唔再靠 Drive 帶 secrets，詳見 `stephanie-personal/docs/NEW-MACHINE-SETUP.md` §9。

> 唔會自動讀入每次對話 context，需要時先 `read_file`。

- **2026-07-25**：**`.active-session.lock*` 冇入 .gitignore → session 鎖檔一直推上 GitHub** **問題**：`session-lock.sh` 喺每個 repo 根寫 `.active-session.lock`；release 嗰陣 Drive mount `rm` 唔到（device bridge 冇 rm 權限），會 fallback 改名做 `.active-session.lock.DELETE-ME-<epoch>`。兩種檔全部 repo 都**冇入 `.gitignore`**，所以 `github_push.py` 照推——最舊一個殘留檔 timestamp 係 **2026-07-14**，即係呢個洩漏行咗成十日。 **修**：12 個 repo（含 `novel-web`）`.gitignore` 全部加 `.active-session.lock*`（一條 pattern 蓋埋活鎖同 `.DELETE-ME-*`）；現存 16 個殘留檔 mv 入各自 `_to_delete/`。 **同類第三宗**：同日先修咗 ①`_to_delete/` 冇入 ignore、②`.bak-*` 冇入回收筒，今次係 ③鎖檔。三宗共通根因＝**新產生嘅暫存檔冇人幫佢配 ignore rule**。 ⚠️ **未做（要 Stephanie 拍板）**：真正治本係改 `session-lock.sh`，唔好將鎖寫入 repo 樹，改寫去 `stephanie-personal/scripts/.session-locks/<repo>.lock` 集中管——咁就冇檔會落 repo，亦唔使靠 12 份 `.gitignore` 各自記得。

- **2026-07-25**：**`github_push.py` 全 repo 統一放 `scripts/`，刪走 root 重複 copy** **問題**：`sales-trainer`／`Travel App`／`daily-novel` 各有兩份 `github_push.py`（root ＋ `scripts/`），內容唔同版本；`novel-web` 就淨得 root 一份。而 `auto_push.sh` 搵 script 係 **root 優先**，制度正本（`PUSH-SETUP.md`／`repo-setup` skill／`06-STANDARDS` §S1）**三處都寫 `<repo>/scripts/github_push.py`**——即係 daemon 同 session 各行各路，drift 咗都冇人知（同日先撞到 4 份仲留住已拆嘅 `sync_local_head()`）。 **修**：① root 嗰份 mv 入 `_to_delete/`；② `novel-web` 由 root 搬去 `scripts/`（同時換返 `scripts/` 版嘅 `REPO = dirname(dirname(__file__))`，root 版計法放錯位會指去上一層）；③ `auto_push.sh` 第 40–41 行次序反轉做 **`scripts/` 優先、root 做 fallback**；④ 各 repo CLAUDE.md 嘅 push 路徑記錄同步改（Travel App 原本寫「root 嗰份係舊 copy」——實情當時 root 先係新版，記錄一直係反嘅）。 **核實**：全 `dev/` 12 份 `github_push.py` 全部喺 `scripts/`、`REPO` 路徑計法逐個對過；`bash -n auto_push.sh` 過。 **餘下良性差異**（唔動）：`catnu-app` 多 `bootstrap_empty_repo`（空 repo 首推）、`venturenix-lab-seminar` 多 `parse_args`、`AI for elderly`／`stephanie-portfolio`／`MakeMyHome`／`fable-prompt` 只差註釋長短。

- **2026-07-25**：**`github_push.py` 版本 drift → 同步返正本，拆走 `sync_local_head()`** **問題**：`scripts/github_push.py` 同 `novel-web/github_push.py`（daily-novel root 嗰份本身已經係新版） 仲行緊舊版 script——有 `sync_local_head()`（push 完 `git fetch` + `git reset --mixed` 令 `git status` 睇落乾淨），亦冇 2026-07-15 加嘅 concurrent-push 警報。但正本喺 **2026-07-15 已經拆走 `sync_local_head()`**：`.git` 住喺 Google Drive streaming 資料夾，嗰兩個 git **寫入**指令成日撞 stale ref lock（`refs/heads/main.lock`），一卡幾十個鐘，令人誤以為「push 唔到」。呢幾個檔等於仲留住當初出事嗰條路徑。 **修**：`scripts/` 嗰份用正本覆蓋；`novel-web/` 嗰份要 root 路徑版，用 `sales-trainer/github_push.py`（同樣冇 `sync_local_head`、有 concurrent 警報）覆蓋。順手幫 novel-web `.gitignore` 補 `_to_delete/`（佢係獨立 repo，同日第一輪掃描漏咗）。 改前版本 → `_to_delete/scripts-github_push.py.bak-20260725`。 **核實**：全 `dev/` grep `sync_local_head` 已歸零；`REPO = ` 路徑計法逐個對過（`scripts/` 版用 `dirname(dirname(__file__))`，root 版用 `dirname(__file__)`，放錯位會指去上一層）。 **背景**：全 `dev/` 15 份 `github_push.py` 共 7 個版本。其餘差異屬良性（catnu-app 多 `bootstrap_empty_repo`、venturenix 多 `parse_args`、其他淨係註釋長短），今次只動有 `sync_local_head` 嗰批。 ⚠️ 提醒：`auto_push.sh` 搵 script 嘅次序係 **root 優先、`scripts/` 做 fallback**（第 40–41 行）。
- **2026-07-25**：**散喺 repo 嘅 `.bak-*` 備份推咗上 GitHub → 搬入 `_to_delete/`**——本 repo `.gitignore` 已經有 `_to_delete/`，但備份檔冇搬入去、留咗喺原位，所以 `github_push.py` 照當普通檔上傳；GitHub Git Trees API 核實 remote `main` 實際有：`CLAUDE.md.bak-20260718`（repo 根）。修法：`mv` 入 `_to_delete/`（跟全局規則「清理一律 mv 去該 project 根目錄 `_to_delete/`」），push 後 `github_push.py` 嘅 `deletions` 邏輯自動由 remote 樹刪走，14 日後由 `stephanie-personal/scripts/clean_to_delete.sh` 真刪。背景：同日先修咗 6 個 repo `.gitignore` 漏咗 `_to_delete/`，跟手全 repo 掃多次，發現呢批係另一種漏法——**回收筒 ignore 咗，但檔案根本冇入過回收筒**。⚠️ 只由 HEAD 移除，舊 commit 歷史仍然有；已 grep 過冇 token／secret 值。

- **2026-07-25**：DeepSeek 官方 2026-07-24 停用舊 model 名 `deepseek-chat`/`deepseek-reasoner`（迎代 `deepseek-v4-flash`/`deepseek-v4-pro`）——`novel-web/src/lib/deepseek.ts` 跟住換做 `model: "deepseek-v4-flash"` + `thinking: { type: "disabled" }`（保持原本非thinking快速回覆行為）。已用真 Mac + 真 API key 實測同一個 request 格式：status=200、回覆正常、`reasoning_content` 為空確認 thinking 已關。⚠️ 未做：純機械式 model 名替換，未喺 novel-web 網站實際觸發一次真結局生成流程。 順手同步更新 `README.md` 舊嘅「DeepSeek-V3（deepseek-chat）」描述做 `DeepSeek（deepseek-v4-flash）`。

- **2026-07-23**（第二輪）：**videoclip 首次真實生圖跑通 + 修 Mac python3.9 相容性 bug**——三個 script（`image_gen.py`／`tts_gen.py`／`assemble.py`）用咗 PEP604 `X | None` union type hint,Mac 系統 `python3` 實測係 3.9.6(唔支援,3.10+先得),行 `image_gen.py` 即刻 `TypeError`;修法係三個檔案頂部加 `from __future__ import annotations`(annotation 延遲評估,3.9 都得,唔使逼 Stephanie 裝新 python)。修完後揀第二篇故事(《離婚協議簽完那晚，他訂的還是我愛吃的那家酸菜魚》,林淮溪,追妻火葬場,story_id `631923fb`)寫 storyboard(7 beat,新 protagonist_lock 涵蓋男女主角兩個人嘅描述,seed 51204),`image_gen.py` 實跑 7 beat 全部用真・CF flux-2(冇 fallback sana),已逐張目視驗證:兩位主角喺 7 張圖入面樣貌/衣著高度一致,質感、光線、情緒運鏡都達標,證明 protagonist_lock + 固定 seed 呢個免費方案work。⚠️ tts_gen.py／assemble.py 呢兩步今次未跑,下次繼續。
- **2026-07-23**：**新增 `videoclip/` 免費短片 pipeline**——同 `shortdrama/`（fal.ai 收費真·圖生片）平行嘅零成本版：由 `novel_stories` 短篇故事做劇本，Claude 親手寫 storyboard（`scripts/<story_id>.json`，7 beat），`image_gen.py` 逐 beat call Cloudflare Workers AI `@cf/black-forest-labs/flux-2-klein-9b`（免費層每日 10,000 neurons ≈ 8–10 張，multipart/form-data，1088x1920；CF 失敗自動 fallback pollinations sana），`tts_gen.py` 用 EdgeTTS（免費，普通話女聲，量實際秒數）生旁白，`assemble.py` ffmpeg 做 6 種輪替 Ken Burns 運鏡＋手持晃動＋暗角＋菲林顆粒＋自動分行燒字幕＋過黑轉場，concat 做 `final.mp4`（crf25 ~15MB）。全免費。選材用三準則（具體道具貫穿／情感靠場面唔靠揭底反轉／情節密度低）；首個 storyboard 揀《茶水間那台舊咖啡機》（職場逆襲，story_id `68eae161`）。主角一致性用 `episode.protagonist_lock`（全片統一外貌硬套）＋固定 seed 拉近（免費 model 冇 reference-lock）。image_prompt 結構升級：景別開頭＋表情情緒＋戲劇光線。已驗證：3 script py_compile＋storyboard JSON parse＋轉場光效版實際併片跑通（80.5s/17MB）。⚠️ 待辦：新 prompt＋主角鎖定嘅正式圖待 CF 額度 reset（今日試爆咗）先生。現階段手動觸發、storyboard 送審後生圖；未來自動化（Telegram novel-video-bot 批准掣＋listener＋每日 task＋Supabase novel_video_jobs/storage/heartbeat）待試順咗先做。根 CLAUDE.md 加路由行。
- **2026-07-18**（第二輪）：CLAUDE.md 加「✅ 完成前檢查 DoD」（novel-web 分流驗法／本 repo script 實跑／雙 github_push.py 邊份推邊個＋核實 HEAD）。改前原版＝`.bak-20260718`。
- **2026-07-18**：CLAUDE.md 重複規則段（寫入分流／Git push／AI 制度 router）換做 5 行「⚙️ Standards」block，正本 → `stephanie-personal/docs/ai-governance/06-STANDARDS.md`（skill 系統重整 session）；改前備份 `CLAUDE.md.bak-20260718`。
- **2026-07-17**：**新增 `shortdrama/` 短劇 video pipeline 骨架**——由 novel-web 現有 `novel_stories`（Supabase）文字故事庫做劇本源頭，拆分鏡（storyboard JSON：鏡頭ID／景別／video_prompt／角色／對白／BGM）接 fal.ai（`fal-ai/ltx-2.3/image-to-video` Fast tier）逐鏡生成短劇 video，`assemble.py` 用 ffmpeg 併片。新增 `video_gen.py`（支援 `--dry-run`／`--shot N` 慳成本試跑）、`assemble.py`、`storyboards/`。首個 demo storyboard 揀咗一篇短篇故事（`替嫁先婚後愛`，Voice D，story_id `3d98fe2f-0012-4728-bc67-60533fae70a6`）拆做 10 鏡～49 秒。已驗證：JSON 格式、python 語法、`--dry-run` 實跑通過；實際 call fal.ai 生成片段待 Stephanie 提供 `FAL_KEY` 先可以驗證。根目錄 `CLAUDE.md` 加一行路由。
- **2026-07-15**：**novel-web/CLAUDE.md 瘦身**（10.7KB→5.8KB，跟 04-MAINTENANCE §0/§4）——歷史敘事（改名史、Hero 插畫試驗史、花磚配色定案史、story_type 加入史、結局生成改版史、`/my-endings` 書脊格仔改版史）全部搬去本檔（見下面幾條 2026-07-05／07-10／07-11 條目），novel-web/CLAUDE.md 淨留路由行＋現行規則。順手幫 novel-web 登記入 `stephanie-personal/scripts/autopush-registry.txt`（之前一直冇喺 daemon 名單入面）。
- **2026-07-15**：**novel-web 加 `/admin` 刪除故事功能**——新增 Admin-only 頁面（`src/app/admin/page.tsx`＋`src/components/AdminStoryList.tsx`＋`src/app/actions/admin.ts`），淨 `auzistephanie@gmail.com` login 見到同用得（`src/lib/admin.ts` 白名單，server action 再驗證一次身份）。刪除機制唔用 service-role key，改喺 Supabase 加 DELETE RLS policy（`novel_stories_admin_delete_and_cascade` migration，淨限 admin email）＋將 `novel_likes`／`novel_endings` 外鍵改做 `ON DELETE CASCADE`，撳刪除會自動連帶清走相關鍾意記錄同讀者專屬結局。`NavBar.tsx` 加條件顯示嘅「Admin」連結。順手發現＋更正 novel-web 個推送方式文件漂移：`novel-web/CLAUDE.md` 舊寫「用普通 git CLI」，但 repo 入面其實已經有 `github_push.py`（同 daily-novel 做法一致），兩份 `CLAUDE.md` 已更新反映真實推送方式；雲端 sandbox 對 novel-web repo 用 API push 會撞 403（session 未 allowlist、但 git clone/push 走 git protocol唔受影響），已記錄呢個限制同正確 fallback（`device_commit_files` 交返本機跑）。cloud sandbox `npm run build` 已驗證通過先推送。
- **2026-07-15**：**close 埋 07-14 條「⚠️ 未完」手尾**——雲端 watchdog 讀取端（`stephanie-personal/idea_inbox/check_novel_story_heartbeat.py`，掛喺 idea-inbox-digest 尾、經 Theo／@auideabot 通知）已經改讀 Supabase `public.service_heartbeat`（原本讀緊 venturenix Google Sheet「Service Heartbeat」tab，07-14 個 write target 改咗之後 Sheet 嗰行冇人再更新，令 watchdog 一路誤報 stale）。改動喺 `stephanie-personal` 做（唔喺呢個 repo），詳情見該 repo `CHANGELOG.md` 同日「同日修正」條目。至此 novel-story-generator 嘅 heartbeat write（Supabase）同 read（Theo watchdog）兩端終於對得上。
- **2026-07-14**：**`novel-story-generator` heartbeat 改用 Supabase**——原 step 4 靠 `venturenix-lab-seminar` 嘅 `core.heartbeat`＋gspread 寫 Google Sheet「Service Heartbeat」tab，但 novel session 只 mount `daily-novel`、接觸唔到 venturenix core 模組同憑證，連續多次 `ModuleNotFoundError` fail。Fix：新增 Supabase 表 `public.service_heartbeat`（task_name PK / last_beat_at / note），step 4 改為一句 `execute_sql` upsert（`on conflict do update`），零外部依賴、逢跑必成；已實測兩次 upsert 只 1 行、時間戳正確更新。⚠️ **未完**：雲端 watchdog 讀取端仍讀 Google Sheet，未改為讀 Supabase `service_heartbeat`，待下次 mount venturenix folder 更新；在此之前 heartbeat 有寫入但無人監察。順手清 root `CLAUDE.md` line 15 對已刪 `novel-ending-generator` task 嘅殘留引用。
- **2026-07-12**：搬機至 Google Drive Mirror——canonical 路徑由 `~/daily-novel` 改為 `~/Desktop/Stephanie-Google Drive/dev/daily-novel`；`novel-web/node_modules`（462M）冇經網絡重裝，喺新位置本地 rsync 複製；`stephanie-personal/scripts/autopush-registry.txt` 已更新新路徑；HEAD／`git fsck`／檔案數（787 個，唔計 node_modules）全部核對通過；舊 `~/daily-novel` 已加 `_MIGRATED_TO_GOOGLE_DRIVE.txt` 標記，確認後可刪。
- **2026-07-11**：**novel-web `/my-endings` 改用書脊格仔版面**——原本長 timeline 一路 scroll 太難搵返舊結局，改用 `src/components/EndingBookshelf.tsx`（client component）：結局橫排做直條書脊（`.spine`／`.spines-wrap`，樣式喺 `globals.css`），撳一下先展開嗰篇詳情（故事節錄／你嘅選擇／專屬結局），一次淨開一本。（呢條同 novel-web/CLAUDE.md 2026-07-15 精簡搬過嚟）
- **2026-07-11**：**移除 Telegram bot，repo 收斂為 novel-web wrapper**——`novel_generator.py`／`lit_generator.py`／`bot_listener.py`／`utils.py`／`webhook_server.py`／`check_errors.py`／`sync_commands.py`＋logs／`Procfile`／`genre_data.json`／`assets`／`novel-landing.html` 等搬去 `_to_delete/bot-removal-2026-07-11/`（已 `git rm --cached` untrack＋`.gitignore` `_to_delete/`，磁碟保留做 backup）；LaunchAgent `com.stephanieau.novel-bot` 待 `launchctl unload`＋刪 plist。產品出口全歸 novel-web（Next.js＋Supabase，Cowork task 生成寫 Supabase）。屬今次跨 repo 上線前安全審計 + 精簡嘅一部分。
- **2026-07-10**：**顧事 House Style（Voice Profile v1）鎖定＋全出口接軌**——Fable session STEP 0 試讀鎖定三聲道：A 快刀流（serial 男頻）／C 夜行者（都市冷幽默第一人稱）／D 拾光人（short 情感，突襲式淚點）；「具體物件驅動」硬規按 Stephanie 要求剔除。正本 `novel-web/VOICE_PROFILE.md`。首批 Fable 6 篇（正本 `novel-web/content/fable_batch_2026-07-10.md`，附連載後續大綱＋分支種子）已入 Supabase `novel_stories`。接軌：① Cowork `novel-story-generator` task prompt 內嵌 Voice Profile 精華＋三聲道分配表（原有類別池／比例／字數／吸引力硬規保留）；② novel-web `endings.ts` 加 `VOICE_RULE` 注入 DeepSeek 結局 system prompt（承接聲道、末句短可獨立引用），syntax 驗證通過，commit a539f37 已 push（Vercel 自動 deploy）。
- **2026-07-10**：`github_push.py` 修好「push 完 `git status` 仍顯示大量假改動」——script 經 GitHub API 寫 remote、唔 advance 本地 HEAD，令 status 成日同舊 HEAD 比。Fix：push 成功後加 `sync_local_head()`（帶 token fetch + `git reset --mixed` 對齊；token 唔寫檔唔 print、失敗唔阻塞、只郁 HEAD+index）；已清殘留 `.git/index.lock` 並驗證 status 歸零。（novel-web/github_push.py 同步套用）
- **2026-07-10**：**novel-web 結局生成改用 DeepSeek 即時生成**——「鍾意」唔再自動觸發結局生成；改為讀者揀完連載故事 → `EndingFlow.tsx` 叫 `endings.ts::getChoices`（DeepSeek 即場生成 3 個劇情分支俾讀者揀）→ 揀一個 → `generateEnding`（DeepSeek 生成 400–700 字專屬結局，insert 入 `novel_endings`）。舊機制（Cowork scheduled task `novel-ending-generator` 每日 17:14 掃 `novel_likes` 缺結局嘅組合、用 Claude 生成冇揀分支嘅結局）已刪除——同新 flow 重疊，會生成無分支根據嘅結局，同新 schema（多咗 `choice_text` 欄）唔一致。（呢條同 novel-web/CLAUDE.md 2026-07-15 精簡搬過嚟）
- **2026-07-05**：**novel-web 改名「顧事」+ 短篇/連載分流**——App 名由「爽文快遞」改做「顧事」（同「故事」國語粵語同音，雙關「照顧/講究事情」）；全站 metadata／NavBar／Footer／Hero SVG／login page 同步改名。`novel_stories` 加 `story_type` 欄位分流：`serial`（連載爽文 1200-1800字，cliffhanger 收尾）／`short`（完整短篇 1500-2200字，有頭有尾，原本 1000-1500 字調高），首頁按呢個欄位分兩個 section。短篇情感深度：Stephanie 反饋短篇「太簡單」，喺 `novel-story-generator` prompt 加四項硬性要求（揀定單一主導情感基調／用具體場景對白物件推進情感禁止「他很感動」式概括敘述／結尾要有明確情感爆發點淚點收筆句／情感層次要有轉折唔可以由頭到尾同一種情緒）。（呢條同 novel-web/CLAUDE.md 2026-07-15 精簡搬過嚟）
- **2026-07-05**：**novel-web Hero 插畫 + 花磚視覺定案**——Hero 插畫試過書店招牌（太不相干）／花磚馬賽克摺書（太肉酸）／純花磚板＋印章／手畫 SVG 貓仔瞓書堆（Stephanie 反饋「太抽象」），最終定案用 Stephanie 提供嘅 AI 生成貓仔瞓書堆貼紙圖（`public/hero-cat.png`，透明背景，`next/image` 顯示）；圖片來源／授權由 Stephanie 確認（話係「AI 生成」），日後換圖直接換呢個檔案（記得先確認新圖版權）。花磚圖案：提議 3 個視覺升級方向（A 典藏書卷最豐富／B 輕奢克制／C 復古藏書票），Stephanie 揀咗 B——雙層菱格紋（外層靛藍描邊＋內層酒紅 `#7a3b32` 描邊＋磚紅中心點），套用晒 `TileDivider.tsx`／`globals.css` 嘅 `.tile-pattern-bg`／`.tile-pattern-vertical`，加埋 `genreColor.ts` 第 5 隻色「酒紅」。跟住 Stephanie 反饋花磚太隱（sidebar 淨 5% opacity 幾乎睇唔出），圖案 opacity 加高（sidebar `NavBar.tsx` `.05`→`.16`、login page `.08`→`.18`），全站底色由米黃 `#f6efe0` 轉白色 `#ffffff`（`--color-cream` 保留唔變做「卡片強調色」，原本 `bg-white/60` 嘅卡片全部改實色 `bg-cream`，`TileDivider.tsx` 淺色版 rect 底色維持米黃）；跟住再反饋導覽選單（sidebar／mobile header）唔應該用花磚圖案，`NavBar.tsx` 移除 `.tile-pattern-bg`，改做「書脊金線裝飾」（桌面版 sidebar 右邊 3px 直向漸層條酒紅→燙金→酒紅，logo 底下橫向漸層細線，手機版 header 底部同款橫向線）——確立原則：花磚圖案淨用喺大面積背景／分隔帶（divider、故事牆底紋），窄小 UI 框架位（nav、filter pills）一律用素色/細線裝飾，唔好用重複紋理，避免「太快」（busy）嘅觀感。（呢條同 novel-web/CLAUDE.md 2026-07-15 精簡搬過嚟）
- **2026-06-11**：取消每隔一日 9am 自動生成 cron，改為自己選擇時間按需生成
- **2026-06-11**：刪 6 個男頻冷門類別（醫術流／古言權謀／鑑寶奇才／神豪撒幣／末世崛起／競技熱血）；新增 10 個女頻言情爆款 + channel/weight 加權系統；加爆款標題公式；男女頻分開 prompt 模板。
- **2026-06-23**：刪甜寵逆襲（男頻，市場飽和）、玄學風水（冷門）；新增漫畫感爽文（M, w2）、重生年代稱霸（M, w2）、懸疑言情（F, w2）——對應 2026 短劇爆款趨勢。
- **2026-07-01**：刪重生年代稱霸（風格特殊，整合難度高）；/now 同 /lit 合併為隨機生成；加 CHARACTER_WOUNDS、TWIST_SEEDS、VILLAIN_MOTIVATIONS、SUSPENSE_HOOKS 池提升故事質量。
- **2026-07-02**：**Phase 1 追睇引擎**——由「短篇機」變「追劇機」。新增連載系統（`SERIES_ARCS` 7 條弧線 3-5 集，每集收 cliffhanger）+ 需求驅動（🤩 超好 → 續寫成連載）+ 讀者選擇分支（每集尾 2 選擇改寫下集）。新指令 `/series`。已用家身份實跑驗證：ep1 標題+選擇掣、ep2 接住讀者選擇、ep3 終集完結，劇情連貫、無殘留控制碼。對應 2026 短劇「密集反轉 + 追更留存」趨勢。
- **2026-07-02**：**Phase 2 題材+節奏**——GENRES 加末世腦洞(M,w2)／都市情緒流(M,w2)／雙強對峙(F,w2)（對應 2026 男頻腦洞末世＋都市情緒、女頻雙強爆點），共 28 類。新增 `HOOK_DENSITY_RULE` 注入單篇男女 prompt，令單篇都有短劇密集節奏。
- **2026-07-02**：**Phase 3 留存數據**——追蹤 start/continue/choice/complete 點擊事件（Redis `retention_metrics`），選類由評分驅動升級「追更率驅動」（`_retention_multiplier`＋`weighted_choice_retention`），`/stats` 加追更率／完讀率／互動率面板。離線驗證：埋點正確、黐人題材倍數 1.68、抽樣被選 3.2 倍。另加 `sync_commands.py` 解決改指令後 Telegram menu 唔更新（setMyCommands 獨立同步、毋須重啟 bot）。
- **2026-07-04**：**連載選擇對齊 + 防重複補強**——① 連載每集尾加 `<<<SCENE>>>` 控制碼強制模型先白描返正文最後一幕先出 NEXT/CA/CB，杜絕「選擇同正文脫節」；生成後用 bigram 重疊率驗證 CA/CB 有冇對應正文尾段，兩個都唔夾先觸發低溫度細 call 補救（`_choice_grounded`／`_regen_ending_hook`）。② 主角職業/性格/傷口（`generate_character`）同反派原型/開場鉤子（`generate_story`）套用返現有 `recent_dna` 防重複機制（sliding window 5），解決短期內故事「似返之前」問題。已用 mock Redis + 模擬 raw output 跑腳本驗證：parsing 正確、grounding 判斷符合預期、8 次連續生成 window-5 內無重複。
- **2026-07-04**：**連載清空功能**——`utils.clear_all_ongoing_series()` 一次過 DEL 晒全部 `series:{id}` + 清走 `series:ongoing` 索引；`/series` 列表加「🗑 清空全部」按鈕，撳咗要二次確認（✅確定／❌取消）先真係執行，避免手快清錯。已用 mock Redis 跑腳本驗證：SMEMBERS 讀取 + 逐個 key DEL + 索引 DEL 全部正確。
- **2026-07-04**：**煞停位吸引力提升 + 「換個新故事」順便清走**——① 加 `CLIFFHANGER_TYPES`（6 種煞停機制：秘密揭穿／身份曝露／抉擇無回頭／意外人物／死線倒數／動作未做）＋ `_pick_episode_hook_elements()` 每集獨立揀type＋`suspense_hook`（同 `recent_dna` 防重複機制，唔影響系列固定嘅 setting/writing_style），杜絕次次都係同一種煞停模式；`_build_episode_prompt` 加「賭注升級」規則（每集緊張感要比上集大）+ 動態 demo（跟返指定類型）。② `utils.delete_series()`：「🎲 換個新故事」callback_data 帶埋 series id，撳咗會先刪走現有系列先開新嘅，唔會累積殘留系列。已用 mock Redis 跑腳本驗證：8 次連續揀 type window-5 內無重複、demo 正確對應指定類型、終集唔受影響、delete_series 三個 Redis call 全部正確。
